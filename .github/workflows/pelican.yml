name: Deploy to GitHub Pages
on:
  push:
    branches: ["main"]
  workflow_dispatch:
jobs:
  deploy:
    uses: "getpelican/pelican/.github/workflows/github_pages.yml@05535c7"
    permissions:
      contents: "read"
      pages: "write"
      id-token: "write"
    with:
      settings: "config-ghpages.py"
